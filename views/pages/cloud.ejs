<div class="flex justify-between p-1">
  <input type="text" class="w-140 border-1 border-gray-900 rounded-full" placeholder="Search">

  <!-- Upload Button + Dropdown (on hover) -->
  <div class="relative group flex flex-col gap-3 w-36 z-10">
    <!-- Upload Button -->
    <button class="px-4 py-2 rounded-lg border-2 border-blue-950">
      Upload
    </button>

    <!-- Dropdown Menu (hidden by default) -->
    <div class="absolute hidden bg-white border border-gray-200 rounded-lg shadow-lg group-hover:block right-0 top-full flex-col">
      <a href="#" class="block px-4 py-2 text-gray-700 hover:bg-gray-100">Upload Folder</a>
      <a href="#" class="block px-4 py-2 text-gray-700 hover:bg-gray-100">Upload Item</a>
    </div>
  </div>
</div>
